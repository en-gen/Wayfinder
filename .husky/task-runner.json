{
  "tasks": [
    {
      "name": "dotnet-format",
      "command": "dotnet",
      "args": ["format", "src/CaseFlow.sln", "--include", "${staged}", "--no-restore"],
      "include": ["**/*.cs", "**/*.csproj"]
    }
  ]
}
