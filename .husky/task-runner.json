{
  "tasks": [
    {
      "name": "dotnet-format",
      "command": "dotnet",
      "args": ["format", "src/Wayfinder.sln", "--include", "${staged}", "--no-restore"],
      "include": ["**/*.cs", "**/*.csproj"]
    }
  ]
}
